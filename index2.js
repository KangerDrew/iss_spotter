const { nextISSTimesForMyLocation, nextISSTimesForMyLocation2 } = require('./iss_promised');


nextISSTimesForMyLocation()
  // .catch((error) => {
  //   console.log("It didn't work: ", error.message);
  // })

